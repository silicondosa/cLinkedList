\label{index_md_README}%
\Hypertarget{index_md_README}%
This is a simple linked list manager for C that I\textquotesingle{}ve been using for a long while now. It may not be as optimized as Boost\textquotesingle{}s libraries for linked lists, but it is very simple to use and has been tested rigorously.

This library allows you to dynamically create and manage doubly linked lists of elements that already exist in memory using pointers. This means that the actual data is untouched by this library which essentially manages a linked list of pointers to data.\hypertarget{index_autotoc_md1}{}\doxysection{Supported platforms and binaries}\label{index_autotoc_md1}
In addition to the source, you can download {\ttfamily c\+Linked\+List} as a precompiled library. It\textquotesingle{}s currently {\itshape only available as a static library for Windows (WIN32, x86)}, but I will soon make a DLL available too.

At some point, I will also build shared and static libraries for 64-\/bit PC Linux.\hypertarget{index_autotoc_md2}{}\doxysection{Usage}\label{index_autotoc_md2}
Download the repo and place it in your project directory. Be sure to add the {\ttfamily include} and {\ttfamily bin} directories of the project to your compile and linker paths respectively. You do not need the {\ttfamily source} and {\ttfamily c\+Linked\+List} directories which can be safely remove.\hypertarget{index_autotoc_md3}{}\doxysection{Supported functions}\label{index_autotoc_md3}
\hypertarget{index_autotoc_md4}{}\doxysubsection{1. Linked list creation functions}\label{index_autotoc_md4}

\begin{DoxyItemize}
\item Create and {\itshape {\bfseries{initialize}}} a linked list in dynamic memory.
\item {\itshape {\bfseries{Insert a data item AFTER}}} an existing linked list element.
\item {\itshape {\bfseries{Insert a data item BEFORE}}} an existing linked list element.
\item {\itshape {\bfseries{Append a data item}}} to the end of a linked list.
\item {\itshape {\bfseries{Prepend a data item}}} to the start of the linked list.
\end{DoxyItemize}\hypertarget{index_autotoc_md5}{}\doxysubsection{2. Linked list reading functions}\label{index_autotoc_md5}

\begin{DoxyItemize}
\item Get the {\itshape {\bfseries{first list element/data item}}} of a linked list.
\item Get the {\itshape {\bfseries{last list element/data item}}} of a linked list.
\item Find the {\itshape {\bfseries{next list element/data item}}} to a specific element of a linked list.
\item Find the {\itshape {\bfseries{previous list element/data item}}} to a specific element of a linked list.
\item {\itshape {\bfseries{Find the list element}}} associated with a particular data item (if linked).
\item {\itshape {\bfseries{Find the data item}}} in a list if it has been linked.
\end{DoxyItemize}\hypertarget{index_autotoc_md6}{}\doxysubsection{3. Linked list status functions}\label{index_autotoc_md6}

\begin{DoxyItemize}
\item {\itshape {\bfseries{Get the length}}} of the linked list.
\item Is the linked list {\itshape {\bfseries{empty?}}}
\end{DoxyItemize}\hypertarget{index_autotoc_md7}{}\doxysubsection{4. Linked list unlinking functions}\label{index_autotoc_md7}

\begin{DoxyItemize}
\item {\itshape {\bfseries{Unlink a particular list element}}} in a linked list.
\item {\itshape {\bfseries{Unlink a list element pointing to specific data item}}} in a linked list.
\item {\itshape {\bfseries{Unlink all list elements}}} from a linked list.
\end{DoxyItemize}\hypertarget{index_autotoc_md8}{}\doxysection{Documentation}\label{index_autotoc_md8}
TBD -\/ Doxygen auto-\/generated documents to be added soon.\hypertarget{index_autotoc_md9}{}\doxysection{Licensing}\label{index_autotoc_md9}
This library and its source code is distributed under the \href{https://choosealicense.com/licenses/mpl-2.0/}{\texttt{ Mozilla Public License v2.\+0}}. 